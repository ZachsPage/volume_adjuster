# Zachs Hello World Example

Starts a FreeRTOS task to print "Hello World"... without the fluff

- Modified the build system to not hide functionality
- Changed CMakelist to only build minimum components
